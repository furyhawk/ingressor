[project]
name = "ingressor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "furyhawk", email = "furyx@hotmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "marker-pdf>=2.0.0",
    "streamlit>=1.62.0",
    "streamlit-ace>=0.1.1",
]

[project.scripts]
ingressor = "ingressor:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
